import React from 'react'

const Title = () => {
  const headingTitle = {
    'marginTop': '0.5rem',
  }

  return (
    <h1 style={headingTitle}>I Love You to the Moon and Back</h1>
  )
}

export default Title